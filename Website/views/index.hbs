<div class="content">
  <h1>{{title}}</h1>
  <p>Hello</p>
  <div class="tweets">
    <table class="table">
      <thead>
        <th>Tweet</th>
        <th>Tweet URL</th>
        <th>Date Posted</th>
      </thead>
      <tbody>
        {{#each tweets}}
          <tr>
            <td>{{this.body}}</td>
            <td><a href="{{this.link}}">View Description here</a></td>
            <td>{{this.postedTime}}</td>
          </tr>
        {{/each}}
      </tbody>
    <table>
  </div>
</div>
